%% -*- mode: erlang -*-
{metrics,
 [ %% {Name, Period in Seconds, MFA}
   {"questions", 1, {folsom_metrics, get_metric_value, ["counter"]}}
 ]
}.
{compile_cmds, ["make", "make rel"]}.
{start_cmds, ["make start"]}.
{cookie, 'm8ball'}.
{auto_node_names, true}.
